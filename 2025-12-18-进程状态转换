1.并发进程对共享资源的操作结果与执行速度有关，在SJF调度算法中，执行速度会直接影响进程的调度顺序，在FCFS、优先级和RR调度算法中，执行速度可能间接改变进程操作共享资源的时机，最终影响结果。
2.系统进程请求一次IO操作，进程状态从运行态变为阻塞态，系统进程所请求的一次IO操作完成后，进程状态从阻塞态变为就绪态。
3.就绪态，运行态，阻塞态之间的状态转换，只有运行态到阻塞态是由进程自身决定的，当进程在运行时需要等待某类资源或时间，会主动发起阻塞请求，放弃CPU并进入阻塞态，
  就绪态->运行态：由调度算法选择就绪队列中的进程分配CPU，
  运行态->就绪态：CPU时间片用完或有更高优先级进程进入就绪队列导致处理机被抢占。
  阻塞态->就绪态：进程等待的资源或事件完成后，由操作系统唤醒。
