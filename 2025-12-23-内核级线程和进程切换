1.同一进程内的所有线程共享该进程的的地址空间和资源，便于该进程内的线程间通信，但会降低进程的安全性。
2.当一个进程到达自然结束点/出现无法克服的错误/被操作系统终止/被其他有终止权的进程终止，会进入终止状态。
  终止状态的过程：(1)等待操作系统回收内存空间等资源，(2)将进程的PCB清零，并将PCB空间返还给操作系统。
3.处理机空闲会直接触发进程调度，此时若就绪队列非空，则会根据调度算法为一个进程分配CPU，若处理机空闲是因为前一个进程结束或阻塞，则该过程属于进程切换，若就绪队列为空，会调度空闲进程占用CPU，但这不属于进程切换。
  一个进程由创建状态进入就绪态/一个进程由阻塞态进入就绪态，处理机可能不空闲，不一定会发生进程切换。
  一个进程从运行态进入就绪态，满足处理机空闲，且就绪队列里存在其他进程，则一定会发生进程切换。
4.对于内核级线程，当一个线程被阻塞，别的线程还可以继续执行，并发能力强，多线程可在多核处理机上并行执行。
  对于用户级线程，所有管理操作(创建/销毁/切换/调度)都由用户态的线程库完成，并不需要操作系统负责。
  同一进程下线程的切换在用户态下即可完成，系统开销小，但当某个用户级线程被阻塞后，整个进程都会被阻塞，并发度不高，多个线程也不可以在多核处理机上并行执行，因为CPU的调度单位是进程。
