1.在采用优先级进程调度时，运行进程是否一定是系统重优先级最高的进程？
  否，因为优先级最高的进程可能处于阻塞态，此时CPU会在就绪队列中调度一个比该进程优先级低的进程上处理机运行。
2.管道中数据一旦被读出，就会彻底消失，因此只能有一个读进程，但可以有多个写进程。
3.多线程模型：
  (1)多对一模型：多个用户级线程对应一个内核级线程，对这些用户级线程的调度和管理依然在用户态下完成，线程管理开销小效率高，但一个线程在访问内核时发生阻塞，则整个进程都会被阻塞。
  (2)一对一模型：一个用户级线程对应一个内核级线程，当一个线程被阻塞时，可以调度另一个线程，并发度较高，但当线程切换会先从用户态变为内核态，再由内核完成内核线程的上下文切换，最后从内核态返回用户态，开销较大。
  (3)多对多模型：多个用户级线程对应多个内核级线程，一般情况下用户级线程会多于内核级线程。
