# N维数组是机器学习和神经网络的主要数据结构
# 张量表示一个数值组成的数组，这个数组可能有多个维度，张量不仅能精准表达深度学习的多维数组，同时还支持深度学习的高效数值计算
import torch
x = torch.arange(12)
print(x.shape)  # 我们可以通过shape属性来访问张量的形状和张量中元素的总数
x = x.reshape(3,4)  # 调用reshape函数改变张量的形状
print(x)
# 在张量计算中，广播机制是一种自动扩展小维度张量形状，使其与大维度张量匹配，从而实现元素级运算的规则
print(x[1:2])  # 访问张量x的第二行
x[2,3]=0  # 修改张量x中第三行第四列的值
print(x)
